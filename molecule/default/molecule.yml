---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: debian-12
    image: "kirillshtrykov/debian-ansible:12"
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    privileged: true
    pre_build_image: true
  - name: debian-13
    image: "kirillshtrykov/debian-ansible:13"
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    privileged: true
    pre_build_image: true

provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: ../../../
  config_options:
    defaults:
      interpreter_python: auto_silent
